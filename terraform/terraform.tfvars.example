# Required — copy to terraform.tfvars and fill in values
hcloud_token = ""
domain       = "example.com"
ssh_key_name = ""  # Name of an existing SSH key in your Hetzner Cloud account

# Optional — uncomment to override defaults
# location    = "fsn1"
# server_type = "cx22"
